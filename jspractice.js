
  var arr1 = [3,9,15,4,10]
  function times3(array) {
      var newArr = []
      for(let i = 0; i < array.length; i++) {
          newArr.push(array[i] * 3)
      }
      return newArr
  }

  console.log(times3(arr1))

  var arr1 = [3, 9, 15, 4, 10]
  function timesThree (array) {
    var newArr = []
    for (let i = 0; i < array.length; i++) {
      newArr.push(array[i] * 3)
    }
    return newArr
  }
    console.log(timesThree(arr1))
